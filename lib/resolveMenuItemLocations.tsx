import {defineLocations} from 'sanity/presentation'

const menuItemLocations = defineLocations({
  message: 'This is used on all pages. Be careful when editing',
  tone: 'caution',
})

export default menuItemLocations
